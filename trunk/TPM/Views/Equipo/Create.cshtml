@model TPM.Models.Equipo

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Equipo</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.CategoriaId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CategoriaId)
            @{
    var allIdentifications = new SelectList(Model.CategoriaLista, "CategoriaId", "NombreCategoria");
            }
            @Html.DropDownListFor(model => model.CategoriaLista, @allIdentifications)
            
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
